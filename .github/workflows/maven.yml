# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: CI CD

on:
  push:
    branches: [ master, alpha ]
  pull_request:
    branches: [ master, alpha, features/posting_list ]

jobs:
  build:

    runs-on: ubuntu-18.04

    steps:
    - name: Check Out
      uses: actions/checkout@v2
    - name: Set up JDK 11
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'adopt'
        cache: maven
    # Caching dependencies
    - name: Cache Maven packages
      uses: actions/cache@v2
      with:
        path: ~/.m2
        key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
        restore-keys: ${{ runner.os }}-m2
    - name: Build with Maven
      run: mvn -DskipTests=true package --file pom.xml
#
#    - name: [CD] Copy Output
#      run: scp -i .github/workflows/key/jhpark-seoul.pem target/marketing-blog.jar ec2-user@ec2-3-38-88-54.ap-northeast-2.compute.amazonaws.com:blog/app/marketing-blog.jar
#
#    - name: [CD] Stop App
#      run: ssh -i .github/workflows/key/jhpark-seoul.pem ec2-user@ec2-3-38-88-54.ap-northeast-2.compute.amazonaws.com sudo service blog stop
#
#    - name: [CD] Start App
#      run: ssh -i .github/workflows/key/jhpark-seoul.pem ec2-user@ec2-3-38-88-54.ap-northeast-2.compute.amazonaws.com sudo service blog start

